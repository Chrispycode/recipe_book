<div class="d-flex justify-content-between">
  <h2>Rezept bearbeiten</h2>
  <%= link_to "Löschen", @recipe, method: :delete, data: {confirm: "Bist du sicher?"}, class: 'btn btn-outline-danger' %>
</div>

<%= render 'form', recipe: @recipe %>
